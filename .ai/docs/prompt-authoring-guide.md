# Prompt Authoring Guide

When writing prompts for new AI skills in the `.ai/profiles` or `.ai/core` directories:
1. Always start with a definition of the AI persona ("You are an Expert...").
2. Clearly lay out responsibilities using numbered lists.
3. Explicitly state what the AI must NEVER do (boundaries).
4. Define the output format (e.g. strict JSON, Markdown schema).
